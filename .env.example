# کلید Claude API برای لایه ۵ (مهندسی پرامپت).
# اگر تنظیم نشود، بازی در «حالت آفلاین» اجرا می‌شود و لایه ۵ با ارزیاب لوکال کار می‌کند.
ANTHROPIC_API_KEY=

# (اختیاری — برای توسعه‌ی آینده) متغیرهای Supabase برای لیدربورد سراسری.
# بازی بدون این‌ها کاملاً کار می‌کند.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
